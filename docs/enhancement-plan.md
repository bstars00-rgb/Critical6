# 고도화 계획 (Enhancement Backlog)

> 내일부터 시작할 전체 고도화 작업의 우선순위 백로그. 현재 상태: P1~P3 기능 구현
> 완료, 실DB(Supabase) 연결 배포 완료, EN/KO + 다크모드 적용, 와이드 레이아웃 1차 적용.

## A. UX/화면 고도화 (사용성 — 최우선)
- [ ] **반응형 와이드 레이아웃 전면 적용** — 모든 페이지가 큰 화면을 채우도록 그리드 재배치
      (Dashboard 8열 적용됨 → OKR/Board/KPI/CRM/Team도 와이드 최적화)
- [ ] **밀도/타이포 체계화** — 폰트 스케일·여백·카드 크기 토큰화(디자인 시스템화)
- [ ] **OKR Tree 고도화** — 컬럼 정렬(제목/상태/진행률 헤더), 펼침 상태 기억, 인라인 편집,
      KR 클릭 시 상세, 드래그로 정렬 변경
- [ ] **Action Board** — 실제 드래그앤드롭(@dnd-kit), 버킷/라벨 필터 바, 카드 상세 Drawer
- [ ] **빈 상태(empty state) 디자인** — 운영 시작 시 안내/CTA(첫 OKR 만들기 등)
- [ ] **로딩 스켈레톤** — Spinner 대신 콘텐츠형 스켈레톤
- [ ] **모바일/태블릿 대응** — 사이드바 접힘, 반응형 표

## B. 관리(Admin) — "개발 없이 운영" 완성
- [ ] **앱 내 Admin 콘솔** — 사용자 role 변경, 팀 생성/배정, 초대 (지금은 Supabase 테이블 직접 수정)
- [ ] **온보딩 마법사** — 첫 admin 로그인 시 회사/팀/분기 셋업
- [ ] **데이터 소스 관리 UI** — data_sources CRUD + 연결 테스트(어댑터 계층 이미 있음)

## C. AI 고도화 (차별점)
- [ ] **실제 LLM 연동** — ClaudeAiProvider 구현(엣지 함수로 키 보호) → mock 교체
      (인터페이스/프롬프트 이미 준비됨: src/ai/)
- [ ] **자동 인사이트 배치** — 주기적 위험 감지/주간 리포트 생성(pg_cron / 엣지 함수)
- [ ] **인사이트 → 액션 전환** — AI 추천을 클릭 한 번으로 Action Plan/Critical 6 생성

## D. 데이터/자동화 (P4)
- [ ] **KPI 라이브 동기화** — SQL/REST 어댑터 서버 구현 + 스케줄러
- [ ] **알림 센터** — 인앱 알림 UI + 규칙(마감 임박/CFR 미작성/blocker), 추후 Slack/Teams
- [ ] **활동 로그 뷰어** — activity_logs 타임라인 화면

## E. 품질/성능
- [ ] **코드 스플리팅** — 현재 단일 번들 ~1MB → 라우트별 lazy import, Recharts/FullCalendar 분리
- [ ] **테스트 확장** — 현재 32(순수 로직). 컴포넌트 테스트(RTL) + 핵심 플로우 E2E(Playwright)
- [ ] **DB 통합 테스트** — 로컬 Supabase 기동 후 RLS/트리거 실증(현재 미검증 영역)
- [ ] **접근성(a11y)** — 키보드 포커스, aria, 대비

## 빠르게 시작하기 좋은 순서 (제안)
1. **A: 와이드 레이아웃 전면 + OKR/Board 고도화** (체감 효과 가장 큼)
2. **B: Admin 콘솔** (실운영 필수)
3. **C: Claude 연동** (AI 진짜로 작동)
4. **E: 코드 스플리팅 + 테스트**

## 알려진 미해결/주의
- 실DB 통합(로그인→CRUD→RLS) **런타임 실증은 Docker 없어 아직 미완** — 실제 가입/사용으로 검증 필요
- 운영 DB는 **빈 상태로 시작**(데모 SEA 데이터 없음). 시작 데이터 필요 시 별도 시드.
- 번들 단일 청크(~1MB) — B/E에서 분리 예정
- tsconfig `noUnusedLocals` off (정리 대상)
